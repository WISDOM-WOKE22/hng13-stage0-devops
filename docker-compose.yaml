version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: my-html-site
    ports:
      - "8080:80"   # maps http://localhost:8080 → container port 80
    volumes:
      - ./html:/usr/share/nginx/html:ro  # serve files from ./html directory (read-only)
    restart: always
